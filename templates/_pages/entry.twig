{# Entry Router #}
{# ---------------------------------------------------------------------- #}

 {{ include([
   '_pages/' ~ entry.section.handle ~ '/' ~ entry.type.handle,
   '_pages/' ~ entry.section.handle ~ '/default',
   '_pages/errorPages/' ~ entry.slug,
   '_pages/errorPages/404',
 ]) }}
