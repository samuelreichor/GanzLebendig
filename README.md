# Starting a Craft CMS Project

## Setup:

### Clone Git Repo

```
git clone git@github.com:samuelreichor/...
```

### Run the following commands

```
ddev start && ddev composer install && npm install && npm run build
```
